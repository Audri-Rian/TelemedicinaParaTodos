# =============================================================================
# Deploy PC2 (Application Node) — copie para .env e preencha os valores.
# Uso: cp .env.example .env && nano .env && docker compose up -d
# =============================================================================

APP_NAME="Telemedicina Para Todos"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost

# Gere a chave com: php artisan key:generate --show (na raiz do repo)
# Ex.: APP_KEY=base64:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx=

# -----------------------------------------------------------------------------
# Banco de dados (PostgreSQL). No Docker os serviços usam hostnames: postgres
# -----------------------------------------------------------------------------
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=telemedicina
DB_USERNAME=postgres
DB_PASSWORD=

# -----------------------------------------------------------------------------
# Redis (cache + sessão). No Docker: host redis
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null
CACHE_STORE=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# -----------------------------------------------------------------------------
# Filas. Padrão: redis. Opcional: rabbitmq (use QUEUE_CONNECTION=rabbitmq)
# -----------------------------------------------------------------------------
QUEUE_CONNECTION=redis
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=telemedicina
RABBITMQ_PASSWORD=
RABBITMQ_VHOST=/

# -----------------------------------------------------------------------------
# Reverb (WebSocket). REVERB_HOST = hostname que o navegador usa (ex. localhost ou domínio)
# -----------------------------------------------------------------------------
BROADCAST_CONNECTION=reverb
REVERB_APP_ID=telemedicina-app
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

# -----------------------------------------------------------------------------
# S3 / MinIO (Storage Node — PC1). Endpoint = API do MinIO no PC1 (ex. http://192.168.1.10:9000)
# Não fixar IP no código; usar apenas esta variável.
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_ENDPOINT=
AWS_USE_PATH_STYLE_ENDPOINT=true

# -----------------------------------------------------------------------------
# SFU (WebRTC) — reservado para uso futuro. Documentar no README.
# -----------------------------------------------------------------------------
# SFU_URL=
# SFU_WS_URL=

# -----------------------------------------------------------------------------
# Mail (ajuste para produção)
# -----------------------------------------------------------------------------
MAIL_MAILER=log
MAIL_FROM_ADDRESS="noreply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# -----------------------------------------------------------------------------
# Log
# -----------------------------------------------------------------------------
LOG_CHANNEL=stack
LOG_LEVEL=warning
