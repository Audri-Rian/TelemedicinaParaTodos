---
description: Tech Lead SDD — analisa feature, refina requisitos, detecta riscos e gera spec técnica em docs/specs/
argument-hint: 'descrição da feature'
---

Invoque o subagent `tech-lead` passando os argumentos como descrição da feature:

**Feature solicitada:** $ARGUMENTS

O agente deve seguir o pipeline completo: analisar contexto do projeto → perguntas de refinamento → gerar e salvar spec em `docs/specs/`.
